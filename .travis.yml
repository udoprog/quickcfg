language: rust
os:
  - linux
  - windows
  - osx
rust:
  - stable
  - nightly
cache: cargo
branches:
  only:
    - master
script:
  - cargo build --verbose
  - cargo doc --no-deps --verbose
  - cargo test --verbose
env:
  - RUST_BACKTRACE=1